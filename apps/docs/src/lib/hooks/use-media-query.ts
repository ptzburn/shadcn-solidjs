// Authored in the registry so consumers and the docs site share one
// definition; re-exported here to keep the `~/lib/hooks/use-media-query.ts`
// import path that registry components use.
export { useMediaQuery } from "~/registry/hook/use-media-query.ts";
