{
  "workspace": ["./apps/*", "./packages/*"],
  "nodeModulesDir": "manual",
  "tasks": {
    "dev": "deno task --cwd=apps/docs dev",
    "build": "deno task --cwd=apps/docs build",
    "test": "deno test -A",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "check": "deno check ."
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0"
  }
}
